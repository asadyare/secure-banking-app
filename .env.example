# Copy to .env and fill with values from Supabase Dashboard → Project Settings → API
# Restart `npm run dev` after any change.
#
# If sign-up hits “email rate limit”: wait, or in Dashboard → Authentication → Providers → Email
# disable “Confirm email” for dev (fewer emails), or add custom SMTP for higher limits.

VITE_SUPABASE_URL=https://jxjeqfiqcdghydeuqcmv.supabase.co
# Use either the anon "publishable" key name or the legacy name (both work):
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_b1IpSj85eniwdIx2_JsbYQ_dulB7I2_
# VITE_SUPABASE_ANON_KEY=your_anon_public_key_here

# Optional: host port for `docker compose up` when 8080 is already in use
# COMPOSE_PORT=8081





